# MemoLog Configuration
# You can delete this file to use built-in defaults.

[data]
log_path = "logs"

[keybindings.composer]
submit = ["ctrl+s"]
newline = ["enter"]
cancel = ["esc"]
clear = ["ctrl+l"]
